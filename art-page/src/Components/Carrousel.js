import React from 'react';
import { UncontrolledCarousel } from 'reactstrap';


const Carrousel = ({items}) => <UncontrolledCarousel className="bg-primary" items={items} />;

export default Carrousel;